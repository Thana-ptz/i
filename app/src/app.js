document.addEventListener('DOMContentLoaded', function () {
  const content = document.querySelector('ion-content');
  content.innerHTML += '<p>This content is dynamically added using JavaScript!</p>';
});
